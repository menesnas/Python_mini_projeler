import random

# prime = 1, not prime = 0
# if it is not prime, it finds next prime
# if input value is less than 2, return value is minimum prime value.


"""
nb = int(input("Enter number\n"))
def is_prime(num):
    i = 2
    if nb < 2:
        return 0
    while (i <= (num / i)):
        if(num % i == 0):
            return 0
        i = 1 + i
    return 1

def find_nextPrime(num):
    i = 2
    if num <= 2:
        return 2
    while (is_prime(num) == 0):
        num = num + 1
    print(num)
    return num

find_nextPrime(nb)

"""