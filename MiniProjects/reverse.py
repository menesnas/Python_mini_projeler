# TERS YAZDIRMA ISLEMLERI

"""
cumle = input("Cumle giriniz (TR karakterler olmadan) : ")

def reverse(yazi):
    yazi = yazi[::-1]
    return yazi

hepsi_ters = reverse(cumle)
kelimeler = [] 

for i in cumle.split(' '):
    kelimeler.append(reverse(i))
kelimeler = " ".join(kelimeler)
yazdir = kelimeler

print("Orijinal cumle :\t{} \nHepsi ters sekilde :\t{} \nKelimeler ters sekilde :\t{} \n".format(cumle,hepsi_ters,yazdir))
"""